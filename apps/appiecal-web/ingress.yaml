apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appiecal-web
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  rules:
    - host: appiecal-web.k3s.home.klrnbk.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: appiecal-web
                port:
                  number: 80
