apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appiecal-idp
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  rules:
    - host: appiecal-idp.k3s.home.klrnbk.nl
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: appiecal-idp
                port:
                  number: 80
