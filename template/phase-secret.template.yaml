apiVersion: secrets.phase.dev/v1alpha1
kind: PhaseSecret
metadata:
  name: APPLICATION_NAME-secret-manager
  namespace: default
spec:
  phaseApp: 'PHASE_APPLICATION_NAME'
  phaseAppEnv: 'production'
  phaseAppEnvPath: '/'
  phaseHost: 'https://phase.rpi-2.home.klrnbk.nl'
  pollingInterval: 60
  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: 'phase-service-token'
        secretNamespace: 'default'
  managedSecretReferences:
    - secretName: 'APPLICATION_NAME-secrets' # Name of the Kubernetes managed secret that Phase will sync
      secretNamespace: 'default'
